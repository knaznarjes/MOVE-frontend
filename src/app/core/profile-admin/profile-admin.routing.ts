import { Route } from '@angular/router';
import { ProfileAdminComponent } from './profile-admin.component';

export const profileAdminRoutes: Route[] = [
    {
        path: '',
        component: ProfileAdminComponent
    }
];
